version: '3.3'

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: assigne-app
        working_dir: /var/www
        volumes:
            - ./:/var/www
        networks:
            - assigne-app-network

    nginx:
        image: nginx:alpine
        container_name: assigne-app-nginx
        volumes:
            - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
            - ./:/var/www
        ports:
            - 8000:80
        networks:
            - assigne-app-network
        depends_on:
            - app


    mysql:
        image: mysql:8.0
        container_name: assigne-app-mysql
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: assigne-app
        volumes:
            - mysql-data:/var/lib/mysql
        networks:
            - assigne-app-network

networks:
    assigne-app-network:
        driver: bridge

volumes:
    mysql-data:
        driver: local
